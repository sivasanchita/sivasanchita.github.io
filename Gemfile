source "https://rubygems.org"

# Matches the Jekyll version and plugins GitHub Pages uses to build the live site
gem "github-pages", group: :jekyll_plugins

# Needed to run `jekyll serve` on Ruby 3+
gem "webrick"
